#include<iostream>
#include<vector>
using namespace std;
vector<int>v;
int main() {
	int N, K;
	cin >> N >> K;
	
	for (int i = 0; i < N; i++) {
		for (int j = 0; j < K; j++) {
			int a;
			v.push_back(a);
		}
	}

	cout << "비와이" << endl;

	return 0;
}